import { StrikesuccessDirective } from './strikesuccess.directive';

describe('StrikesuccessDirective', () => {
  it('should create an instance', () => {
    const directive = new StrikesuccessDirective();
    expect(directive).toBeTruthy();
  });
});
